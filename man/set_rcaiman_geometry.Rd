% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_rcaiman_geometry.R
\name{set_rcaiman_geometry}
\alias{set_rcaiman_geometry}
\title{Set rcaiman internal raster geometry}
\usage{
set_rcaiman_geometry(r)
}
\arguments{
\item{r}{numeric \link[terra:SpatRaster-class]{terra::SpatRaster}.}
}
\value{
The same \link[terra:SpatRaster-class]{terra::SpatRaster} with standardized extent and CRS.
}
\description{
Apply the internal raster geometry convention used throughout
\emph{rcaiman}: rasters are treated as image grids, not as spatial objects.
}
\details{
This function enforces a unit-resolution pixel grid with a
standardized extent \code{ext(0, ncol, 0, nrow)} and assigns a non-geographic CRS
as an explicit marker that no spatial semantics are intended. The \code{EPSG:7589}
was selected because it does not have datum, projection, or physical units.
This ensure consistent behavior across \code{terra}-based workflows. Using \code{crs = NA} is intentionally avoided since many \code{terra} methods and external tools
assume a defined CRS and may otherwise fail, emit warnings, or apply implicit
defaults.
}
